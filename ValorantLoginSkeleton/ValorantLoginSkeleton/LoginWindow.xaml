<Window x:Class="ValorantLoginApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="420" Width="380"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundDarkBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="WELCOME" 
                   FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource AccentRedBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,18"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Username" Foreground="{StaticResource ForegroundBrush}"/>
            <TextBox x:Name="UsernameTextBox"
                     Background="#1F1F1F" Foreground="{StaticResource ForegroundBrush}"
                     BorderBrush="{StaticResource AccentRedBrush}" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Password" Foreground="{StaticResource ForegroundBrush}"/>
            <PasswordBox x:Name="PasswordBox"
                         Background="#1F1F1F" Foreground="{StaticResource ForegroundBrush}"
                         BorderBrush="{StaticResource AccentRedBrush}" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="3" VerticalAlignment="Top">
            <Button Content="Login" Click="LoginButton_Click"
                    Background="{StaticResource AccentRedBrush}" Foreground="White"
                    Padding="10" Margin="0,0,0,8"/>
            <Button Content="Create account" Click="SignUpButton_Click"
                    Background="Transparent" Foreground="{StaticResource AccentRedBrush}"
                    BorderBrush="{StaticResource AccentRedBrush}" Padding="8"/>
        </StackPanel>
    </Grid>
</Window>
